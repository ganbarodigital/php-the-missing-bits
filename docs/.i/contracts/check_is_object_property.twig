## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    check_is_object_property()
     [x] returns true for non static public property
     [x] returns true for non static protected property
     [x] returns true for non static private property
     [x] returns false for static public property
     [x] returns false for static protected property
     [x] returns false for static private property

{% include ".i/boilerplate/behavioural-contract.twig" %}
