## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    GanbaroDigital\MissingBits\TypeChecks\IsListOfCallables
     [x] can instantiate
     [x] supports fluent interface
     [x] throws TypeError for non lists bool(true)
     [x] throws TypeError for non lists bool(false)
     [x] throws TypeError for non lists
     [x] returns TRUE for callable arrays callable(static method array)
     [x] returns TRUE for callable arrays callable(object array)
     [x] returns TRUE for callable functions callable(anonymous function)
     [x] returns TRUE for callable objects callable(invokeable object)
     [x] returns TRUE for callable strings callable(built-in function string)
     [x] returns TRUE for callable strings callable(static method string)
     [x] returns FALSE for everything else array(empty)
     [x] returns FALSE for everything else

{% include ".i/boilerplate/behavioural-contract.twig" %}
