## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    GanbaroDigital\MissingBits\TypeChecks\IsDefinedInterface
     [x] Can instantiate
     [x] Can call statically
     [x] Returns true for interface names
     [x] Returns false for class names
     [x] Returns false for trait names
     [x] Returns false for everything else

{% include ".i/boilerplate/behavioural-contract.twig" %}
