## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    GanbaroDigital\MissingBits\ClassesAndObjects\FilterProperties
     [x] returns static properties from classes
     [x] returns static properties from parent classes
     [x] returns static properties from trait used by class
     [x] returns static properties from trait used by parent class
     [x] returns static properties from trait used by class and parent class
     [x] returns non static properties from objects
     [x] returns non static properties from parent classes
     [x] returns non static properties from trait used by object
     [x] returns non static properties from trait used by parent class
     [x] returns non static properties from trait used by object and parent class

{% include ".i/boilerplate/behavioural-contract.twig" %}
