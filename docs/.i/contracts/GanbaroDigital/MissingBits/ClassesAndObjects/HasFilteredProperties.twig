## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    GanbaroDigital\MissingBits\ClassesAndObjects\HasFilteredProperties
     [x] returns false when class has no static properties
     [x] returns true when class has static properties
     [x] returns true when parent classes have static properties
     [x] returns true when class uses trait with static properties
     [x] returns true when parent class uses trait with static properties
     [x] returns false when object has no non static properties
     [x] returns true when object has non static properties
     [x] returns true when parent class has non static properties
     [x] returns true when object uses trait with non static properties
     [x] returns true when object parent class uses trait with non static properties

{% include ".i/boilerplate/behavioural-contract.twig" %}
