## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    GanbaroDigital\MissingBits\TraceInspectors\StackFrame
     [x] Can instantiate
     [x] Stores classname if provided
     [x] Stores method if provided
     [x] Stores call type if provided
     [x] Stores filename if provided
     [x] Stores line number if provided
     [x] Get method returns null if no class provided
     [x] Get function returns function even if no class provided
     [x] Stores call stack if provided
     [x] Can coerce to string

{% include ".i/boilerplate/behavioural-contract.twig" %}
