## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    check_is_array_list()
     [x] array of arrays returns true
     [x] lists containing anything else returns false
     [x] throws InvalidArgumentException if passed non list

{% include ".i/boilerplate/behavioural-contract.twig" %}
