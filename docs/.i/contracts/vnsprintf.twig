## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    Vnsprintf()
     [x] Supports sprintf strings
     [x] Supports named parameters
     [x] Supports named and positional parameters
     [x] Throws exception when named parameter not in args

{% include ".i/boilerplate/behavioural-contract.twig" %}
