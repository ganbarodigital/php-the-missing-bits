## Behavoural Contract

Here is the behavioural contract, enforced by our unit tests:

    check_is_listy_object()
     [x] stdClass returns true
     [x] Traversable returns true
     [x] arbitrary objects return true
     [x] Closure returns false
     [x] anything else returns false

{% include ".i/boilerplate/behavioural-contract.twig" %}
