### Example 1: Check For List Of Arrays

This example shows you how to check for a list of arrays.

```php
$list = [
    [ 'hello, world!' ]
];
$isArray = check_is_array_list($list);
var_dump($isArray);
```
The code above produces this output on PHP v5.6.30, PHP v7.0.20, PHP v7.1.6:

```
bool(true)
```
