### Example 2: Check If Not Array

This example shows you how to spot something that isn't an array.

```php
$isArray = check_is_array('hello, world!');
var_dump($isArray);
```
The code above produces this output on PHP v5.6.30, PHP v7.0.20, PHP v7.1.6:

```
bool(false)
```
