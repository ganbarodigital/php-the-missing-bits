### Example 1: Check If Array

This example shows you how to check for an array.

```php
$isArray = check_is_array([]);
var_dump($isArray);
```
The code above produces this output on PHP v5.6.30, PHP v7.0.19, PHP v7.1.5:

```
bool(true)
```
