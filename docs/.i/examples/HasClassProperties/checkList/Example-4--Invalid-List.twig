### Example 4: Invalid List

This example shows you what happens if you don't pass a valid list in.

```php
use GanbaroDigital\MissingBits\ClassesAndObjects\HasClassProperties;

try {
    $props = HasClassProperties::checkList(
        // definitely not a list!
        'hello, world!'
    );
}
catch (TypeError $e) {
    var_dump($e->getMessage());
}
```
The code above produces this output on PHP v5.6.30, PHP v7.0.20, PHP v7.1.6:

```
string(47) "$list is not a list, is a string<hello, world!>"
```
