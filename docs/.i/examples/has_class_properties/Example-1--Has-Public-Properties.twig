### Example 1: Has Public Properties

This example shows you how to check if a class has public properties.

```php
$props = has_class_properties(ExampleClass::class);
var_dump($props);
```
The code above produces this output on PHP v5.6.30, PHP v7.0.19, PHP v7.1.5:

```
bool(true)
```
