### Example 3: Via Factory Method

The `using()` method is a static factory method. It returns a `ListCheck` object.

```php
$list = [ 15, 17 ];
var_dump(IsInRange::using(10,20)->inspectList($list));
```
The code above produces this output on PHP v5.6.30, PHP v7.0.19, PHP v7.1.5:

```
bool(true)
```
