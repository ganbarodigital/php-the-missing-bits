### Example 2: Get Protected Properties

This example shows you how to get the protected properties of a class.

```php
use GanbaroDigital\MissingBits\ClassesAndObjects\FilterClassProperties;

$props = FilterClassProperties::from(
    ExampleClass::class,
    ReflectionProperty::IS_PROTECTED
);
var_dump($props);
```
The code above produces this output on PHP v5.6.30, PHP v7.0.19, PHP v7.1.5:

```
array(2) {
  ["value2"]=>
  string(28) "I am protected static value2"
  ["trait2"]=>
  string(28) "I am protected static trait2"
}
```
